diff --git a/node_modules/react-native/scripts/react-native-xcode.sh b/node_modules/react-native/scripts/react-native-xcode.sh
index 001b6cb..7a9a76b 100755
--- a/node_modules/react-native/scripts/react-native-xcode.sh
+++ b/node_modules/react-native/scripts/react-native-xcode.sh
@@ -145,7 +145,12 @@ if [[ -n "$CONFIG_JSON" ]]; then
 elif [[ -n "$CONFIG_CMD" ]]; then
   EXTRA_ARGS+=("--config-cmd" "$CONFIG_CMD")
 else
-  EXTRA_ARGS+=("--config-cmd" "$NODE_BINARY $NODE_ARGS $REACT_NATIVE_DIR/cli.js config")
+  # Quote for shell/execSync — paths may contain spaces (e.g. ".../MACBOOK PRO DESKTOP/...").
+  _RN_CONFIG_CMD="$(printf '%q ' "$NODE_BINARY")"
+  [[ -n "$NODE_ARGS" ]] && _RN_CONFIG_CMD+=$(printf '%q ' $NODE_ARGS)
+  _RN_CONFIG_CMD+=$(printf '%q ' "$REACT_NATIVE_DIR/cli.js")
+  _RN_CONFIG_CMD+="config"
+  EXTRA_ARGS+=("--config-cmd" "$_RN_CONFIG_CMD")
 fi
 
 # shellcheck disable=SC2086
diff --git a/node_modules/react-native/scripts/xcode/with-environment.sh b/node_modules/react-native/scripts/xcode/with-environment.sh
index c6bd62c..e615e6f 100755
--- a/node_modules/react-native/scripts/xcode/with-environment.sh
+++ b/node_modules/react-native/scripts/xcode/with-environment.sh
@@ -41,7 +41,7 @@ else
     source "${REACT_NATIVE_PATH}/scripts/find-node-for-xcode.sh"
 fi
 
-# Execute argument, if present
+# Execute argument, if present (must quote — paths may contain spaces)
 if [ -n "$1" ]; then
-  $1
+  "$1"
 fi
